# claude-config

Claude Code instruction files, symlinked into `~/.claude` on each machine.

    git clone https://github.com/JimmyC41/claude-config.git ~/dotfiles/claude
    ~/dotfiles/claude/install.sh

`install.sh` backs up any existing `~/.claude/*.md` before linking, and is safe to re-run.
